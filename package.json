{
  "name": "cbanigan.github.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && eleventy && npm run postbuild",
    "postbuild": "node postbuild.js",
    "serve": "tsc && eleventy --serve",
    "dev": "tsc && eleventy --serve --watch",
    "compile-ts": "tsc"
  },
  "keywords": [],
  "author": "Colin Banigan",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "typescript": "^5.9.3"
  }
}
